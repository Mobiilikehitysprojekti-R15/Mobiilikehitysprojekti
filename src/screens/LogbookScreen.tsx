import { StyleSheet, Text, View } from "react-native";
type Props = {};

const LogbookScreen = (props: Props) => {
  return (
    <View>
      <Text>This is LogbookScreen</Text>
    </View>
  );
};

export default LogbookScreen;

const styles = StyleSheet.create({});