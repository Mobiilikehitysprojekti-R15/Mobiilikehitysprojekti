import { StyleSheet, Text, View } from "react-native";
type Props = {};

const WeatherScreen = (props: Props) => {
  return (
    <View>
      <Text>This is Weather screen</Text>
    </View>
  );
};

export default WeatherScreen;

const styles = StyleSheet.create({});
